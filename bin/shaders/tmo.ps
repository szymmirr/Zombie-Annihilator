#version 400
in vec2 TexCoord;
in vec4 xPosition;

uniform sampler2D tex;
uniform float gamma;
out vec4 outputColor;

void main()
{
		outputColor = pow(vec4(texture(tex, TexCoord).rgb,1), vec4(gamma,gamma,gamma,gamma));
}